!-- --------------------------------------------------------------------
Scenario: Open Webpage
!-- --------------------------------------------------------------------
When I open the page for following address <starturl>
Then the brand of the page is as given
Then the country of the page is as given
Then the site type of the page is as given

!-- --------------------------------------------------------------------
Scenario: Click on Login
!-- --------------------------------------------------------------------
When I click the navigation element myaccountbutton
Then the page 'loginpage' is opened
Then the element 'username' is visible
Then the element 'password' is visible
Then the element 'logInButton' is visible

!-- --------------------------------------------------------------------
Scenario: Enter Credentials and Login.
!-- --------------------------------------------------------------------
When I enter <orderuserttp> into 'username'
Then the form field username is filled correct with the parameter <orderuserttp>
When I enter <orderuserpwd> into 'password'
Then the form field password is filled correct with the parameter <orderuserpwd>
When I click the button 'logInButton'
!-- Needs Testing_id demand.

Then the page 'myaccountpage' is opened
!-- Needs page_id definition "testing_id" 


!-- --------------------------------------------------------------------
Scenario: Logout.
!-- --------------------------------------------------------------------
When I click the navigation element metaNavigation
Then the element metaNavigationFlyout is visible
When I click the navigation element metaLogout
Then the page 'loginpage' is opened


!-- ---- TTP-14879 - TEST Basic LOGIN test on MBMD BALAY is successful -----
 
